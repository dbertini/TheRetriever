package it.retriever.test;

import org.junit.Test;

import it.db.retriever.utils.FileUtils;

public class FileUtilityTest {
	@Test
	public void storeDataTest() {
		//FileUtils.storeDataLog(aReport, aReportStatus);();
		assert(true);
	}
}
